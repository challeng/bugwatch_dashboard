defaults: &defaults
  adapter: mysql2
  username: root
  password:
  host: localhost

development:
  <<: *defaults
  database: bugwatch_dashboard_development

test:
  <<: *defaults
  database: bugwatch_dashboard_test

production:
  <<: *defaults
  database: bugwatch_dashboard_production