Hello,

The following files have been changed by <%= @commit.grit.author.email %> :

<% @file_names.each do |file_name| %>
	<%= file_name %>
<% end %>

Here is a link to the commit:
<%= commit_repo_url @repo, @commit.sha %>

The diff for these files:
<%= @diff_string %>